/*
 * Zabbix Automation Module — Dashboard JS
 * No dynamic behaviour needed on the dashboard; this file is a placeholder
 * for future enhancements (e.g., live refresh of counters via AJAX).
 */

'use strict';

document.addEventListener('DOMContentLoaded', () => {
    // Future: auto-refresh stats every N seconds
});
